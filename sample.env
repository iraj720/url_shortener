# --------- Basic Configuration ---------
# Options are "production", "staging", "dev"
ENV_SETTING=dev

# --------- Database Configuration ---------
# Default: SQLite (file-based database in project root)
# Database file will be created at: ./urlshortener.db
# No server setup required - perfect for local development!

# For PostgreSQL (production), uncomment and set:
# DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname
# Example: DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/urlshortener

# SQLite (default - no configuration needed)
# DATABASE_URL=sqlite+aiosqlite:///./urlshortener.db

# --------- Application Configuration ---------
# Base URL for generating short URLs (used in responses)
BASE_URL=http://localhost:8002

# --------- Short Code Pool Configuration ---------
# Pool size: Number of pre-allocated short codes to maintain in memory
# Larger pool = less frequent refills, but more memory usage
# Default: 1000 codes (supports high concurrency without database contention)
SHORT_CODE_POOL_SIZE=1000

# Refill threshold: Refill pool when it drops below this fraction (0.0-1.0)
# 0.2 = refill when pool has less than 20% codes remaining
# Lower threshold = more frequent refills, but ensures pool never runs empty
SHORT_CODE_POOL_REFILL_THRESHOLD=0.2

# Batch size: Number of IDs to reserve in each batch reservation
# Each worker reserves a range (e.g., IDs 1-10, then 11-20, etc.)
# Default: 10 IDs per batch
SHORT_CODE_BATCH_SIZE=10

# Short code length: Fixed length for all short codes
# All codes will be this length, padded with zeros (e.g., "0000000", "0000001")
# Default: 7 characters
SHORT_CODE_LENGTH=7

# Maximum number of service instances that can register
# Each service gets a unique ID from 1 to MAX_SERVICES
# Default: 100 service instances
MAX_SERVICES=100

# Optional friendly name for this service instance
# Used for identification in logs and monitoring
# Default: None (service will be identified by its ID)
# SERVICE_NAME=my-service-instance

